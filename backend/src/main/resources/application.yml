spring:
  application:
    name: ReserveSpotAPI
  datasource:
    url: jdbc:postgresql://localhost:5432/reserve-spot
    username: hardwak
    password: hardwak
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql

#logging:
#  level:
#    org:
#      springframework:
#        security: TRACE

#autoconfigure:
#  exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
